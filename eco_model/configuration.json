{
    "Data": {
        "Database": "postgres: ecomodel on localhost",
        "Parametertable": "data.parameters",
		"ParameterSQL" : "WITH RECURSIVE tree_search AS (SELECT *, 0 AS \"level\" FROM \"data\".\"parameters\" WHERE \"parent\" IS NULL UNION ALL SELECT t.*, ts.\"level\"+1 AS \"level\" FROM \"data\".\"parameters\" t, tree_search ts WHERE t.\"parent\" = ts.\"name\") SELECT * FROM tree_search ORDER BY \"level\", \"sort\";",
        "Load as layer": false,
        "Item delimiter" : "¤"
    }
}
